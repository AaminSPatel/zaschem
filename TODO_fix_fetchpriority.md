# TODO: Fix fetchpriority React/Next warning

- [ ] Locate all usages of `fetchpriority` (lowercase) on DOM elements.
- [ ] Replace `fetchpriority` with correct React prop `fetchPriority`.
- [ ] Ensure the value remains compatible (e.g., 'high'/'auto').
- [ ] Re-run `npm run dev` to confirm warning is gone.

